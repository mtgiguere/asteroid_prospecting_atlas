# Copy this file to .env and adjust for your environment.
# .env is gitignored — this file documents the available variables.

# Base URL for the FastAPI backend. Defaults to localhost:8000 if not set.
VITE_API_BASE=http://localhost:8000
